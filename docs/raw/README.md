# docs/raw

okf 지식 번들에 반영할 **원자료**를 그대로 넣어두는 곳이다. 회의록, 기획서,
API 스펙 덤프, 스크린샷 설명, 외부 문서 발췌 등 형식은 가리지 않는다.

여기 있는 파일은 okf 문서의 재료일 뿐 결과물이 아니다. 자료를 넣은 뒤
`/okf-ingest` 를 실행하면 `okf/` 의 concept 본문과 `okf/log.md` 에 반영된다.

파일 이름에 날짜와 출처를 남기면 이력을 쫓기 쉽다 — 예: `2026-09-10-급여정책-회의록.md`.
